"""OPTIONAL seq2seq parser (templated language -> program), ablation only.

The core no-text system does NOT use this: structured questions already carry
their programs. This stub exists if you later want to demonstrate parsing
TEMPLATED English (generated from the same templates) as a comparison point.
Not on the critical path.
"""
# Intentionally a stub. Implement only if the templated-language ablation is run.
